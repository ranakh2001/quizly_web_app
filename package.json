{
  "name": "quizly",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Quizly - timed online quiz app for Nour's Tutoring Centre",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "setup": "npm install && npm run migrate -w server && npm run seed -w server",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "start": "npm run build -w client && npm run start -w server",
    "test": "npm run test -w server",
    "lint": "eslint .",
    "format": "prettier --write .",
    "reset-db": "npm run reset-db -w server"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "concurrently": "^9.1.0",
    "eslint": "^9.15.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "globals": "^15.12.0",
    "prettier": "^3.3.3"
  }
}
